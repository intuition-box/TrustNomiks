/** The registry's north-star goal: structured tokens indexed in the graph.
 *  Single source of truth for every "/300" surface (dashboard, screener,
 *  landing). The primary vocabulary is "structured" (a token exists in the
 *  registry); "validated" is the secondary, quality-gated count. */
export const TARGET_TOKENS = 300
